import requests

BASE_URL = "http://127.0.0.1:8000/api"

def run_test():
    print("1. Authenticating as admin...")
    # Wait, what are the credentials? 
    # Usually 'admin' / 'admin' or we can create a superuser using django if it doesn't exist.
    pass
